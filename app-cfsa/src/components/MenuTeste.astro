<button id="mobileMenuButton" class="rounded-lg p-2 hover:bg-gray-100 lg:hidden" aria-label="Toggle menu">
    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
    </svg>
  </button>

  <div id="mobileMenu" class="absolute left-0 top-full z-30 w-full bg-white p-4 shadow-lg lg:hidden hidden">
    <nav>
      <ul  class="space-y-4">

        <li><a href="#" class="block text-gray-700 hover:text-primary">Inicio</a></li>

        <li><a href="#" class="block text-gray-700 hover:text-primary">Matriculas</a></li>
  
        <li class="relative">
          <button class="flex w-full items-center justify-between text-gray-700 hover:text-primary">
            <span>O Colégio</span>
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <ul class="mobileDrop" class="mt-2 space-y-2 pl-4 hidden">
            <li class="mobileDropMenu"><a href="#" class="block text-gray-700 hover:text-primary">drop 1</a></li>
            <li class="mobileDropMenu"><a href="#" class="block text-gray-700 hover:text-primary">drop 2</a></li>
          </ul>
        </li>

      </ul>
    </nav>
  </div>
</div>
</header>

<script>

    const mobileMenuButton = document.getElementById('mobileMenuButton');
    const mobileMenu = document.getElementById('mobileMenu');
    const mobileDrop = document.querySelector('.mobileDrop');
    const mobileDropMenu = document.querySelector('.mobileDropMenu');

    mobileMenuButton?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
    });

    mobileDrop?.addEventListener('click', () => {
    mobileDropMenu?.classList.toggle('hidden');
    mobileDrop.querySelector('svg')?.classList.toggle('rotate-180');
    });

</script>